# Slice 1 reflection

## Learned

A single workspace profile is enough to make the form real. Photo validation belongs in software: magic bytes, size, count, and server-owned keys. Client filenames are untrusted.

Thin-profile warnings should be computed from evidence, not from whether the user clicked Save.

## Reviewer notes to keep

- Do not generate persona yet.
- Keep storage behind `StorageProvider`.
- Do not treat Compose as a substitute for schema validation.

## Next

Slice 2 can add `TextGenerationProvider` without changing the profile aggregate.
